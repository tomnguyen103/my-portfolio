[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: serve index.html for client-side routes (/about, /projects, /contact)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
